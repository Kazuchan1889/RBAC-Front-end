{
    "buildCommand": "npm install -g @angular/cli && ng build --output-path=vercel/output",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}