{
    "buildCommand": "npm install -g @angular/cli && ng build",
    "outputDirectory": "dist/rbac-frontend/browser",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}