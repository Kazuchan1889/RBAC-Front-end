{
    "buildCommand": "mkdir -p vercel/output && npx @angular/cli build && cp -rv dist/rbac-frontend/browser/* vercel/output/",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}