{
    "buildCommand": "npm install -g @angular/cli && npm run build",
    "outputDirectory": "dist/rbac-frontend",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}